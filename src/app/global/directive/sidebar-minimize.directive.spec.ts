import { SidebarMinimizeDirective } from './sidebar-minimize.directive';

describe('SidebarMinimizeDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarMinimizeDirective();
    expect(directive).toBeTruthy();
  });
});
