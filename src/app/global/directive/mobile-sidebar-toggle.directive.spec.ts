import { MobileSidebarToggleDirective } from './mobile-sidebar-toggle.directive';

describe('MobileSidebarToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new MobileSidebarToggleDirective();
    expect(directive).toBeTruthy();
  });
});
