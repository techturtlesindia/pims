import { SidebarOffCanvasCloseDirective } from './sidebar-off-canvas-close.directive';

describe('SidebarOffCanvasCloseDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarOffCanvasCloseDirective();
    expect(directive).toBeTruthy();
  });
});
