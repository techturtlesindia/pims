import { BrandMinimizeDirective } from './brand-minimize.directive';

describe('BrandMinimizeDirective', () => {
  it('should create an instance', () => {
    const directive = new BrandMinimizeDirective();
    expect(directive).toBeTruthy();
  });
});
